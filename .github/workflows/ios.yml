name: iOS workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build, Test and Analyze water-tracker app using the iOS Simulator for iPhone11
    runs-on: macos-11
    env:
      scheme: "water-tracker"
      platform: "iOS Simulator"
      device: "iPhone 11"

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install Dependencies
        run: |
          pod install --repo-update
      - name: Clean
        run: |
          xcodebuild clean -scheme "$scheme" -workspace "$scheme".xcworkspace
      - name: Build
        run: |
          xcodebuild build-for-testing -scheme "$scheme" -workspace "$scheme".xcworkspace -destination "platform=$platform,name=$device"
      - name: Test
        run: |
          xcodebuild test-without-building -scheme "$scheme" -workspace "$scheme".xcworkspace -destination "platform=$platform,name=$device"
      - name: Analyze
        run: |
          xcodebuild analyze -scheme "$scheme" -workspace "$scheme".xcworkspace | xcpretty && exit ${PIPESTATUS[0]}
