name: iOS workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build, Test and Analyze water-tracker app using the iOS Simulator for iPhone11
    runs-on: macos-11
    env:
      scheme: "water-tracker"
      platform: "iOS Simulator"
      device: "iPhone 11"

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install Dependencies
        run: |
          gem env
          gem install xcpretty
          pod install --repo-update
      - name: Clean
        run: |
          xcodebuild clean -scheme "$scheme" -workspace "$scheme".xcworkspace | /usr/local/lib/ruby/gems/2.7.0/bin/xcpretty && exit ${PIPESTATUS[0]}
      - name: Build
        run: |
          xcodebuild build-for-testing -scheme "$scheme" -workspace "$scheme".xcworkspace -destination "platform=$platform,name=$device" | /usr/local/lib/ruby/gems/2.7.0/bin/xcpretty && exit ${PIPESTATUS[0]}
      - name: Test
        run: |
          xcodebuild test-without-building -scheme "$scheme" -workspace "$scheme".xcworkspace -destination "platform=$platform,name=$device" | /usr/local/lib/ruby/gems/2.7.0/bin/xcpretty && exit ${PIPESTATUS[0]}
      - name: Analyze
        run: |
          xcodebuild analyze -scheme "$scheme" -workspace "$scheme".xcworkspace -destination "platform=$platform,name=$device" | /usr/local/lib/ruby/gems/2.7.0/bin/xcpretty && exit ${PIPESTATUS[0]}
